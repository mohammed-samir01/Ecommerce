export class Password {
  private password: any;
  private confirmation_password: any;

  constructor(password: any, confirmation_password: any) {
    this.password = password;
    this.confirmation_password = confirmation_password;
  }
}
